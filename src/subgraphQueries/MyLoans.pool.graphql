query MyLoans($first: Int!, $skip: Int!, $currentAddress: Bytes!) {
  debts(first: $first, skip: $skip, where: {borrower: $currentAddress}) {
    id
    debt_id
    apr
    borrower
    total
    repayed
    pledges
    staked
    status
  }
}