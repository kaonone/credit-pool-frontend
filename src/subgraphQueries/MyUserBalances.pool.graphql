subscription MyUserBalances($first: Int!, $address: String!, $date: ID = "0x00") {
  balances(first: $first, orderBy: id, where: {user: $address, id_gt: $date}) {
    id
    user
    lBalance
    pBalance
  }
}