# TODO where not working without $date
subscription MyUserBalances($first: Int!, $address: String!, $date: ID) {
  balances(first: $first, orderBy: id, where: {user: $address, id_gt: $date}) {
    id
    user
    lBalance
    pBalance
  }
}