query DebtProposals($first: Int!, $skip: Int!) {
  debts(first: $first, skip: $skip, where: {status: PROPOSED}) {
    id
    debt_id
    proposal_id
    apr
    borrower
    total
    repayed
    pledges
    lStaked
    pStaked
    status
    description
    last_update
  }
}