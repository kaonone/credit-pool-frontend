import { makeStyles } from 'utils/styles';

export const useStyles = makeStyles(
  () => ({
    root: {},
    topPart: {},

    bottomPart: {
      fontSize: 12,
    },
  }),
  { name: 'double-line-cell' },
);
