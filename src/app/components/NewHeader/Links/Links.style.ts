import { makeStyles } from 'utils/styles';

export const useStyles = makeStyles(
  () => ({
    root: {
      display: 'flex',
    },

    link: {
      marginLeft: 50,
    },
  }),
  { name: 'Links' },
);
