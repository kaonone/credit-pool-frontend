import { makeStyles, colors } from 'utils/styles';

export const useStyles = makeStyles(() => ({
  icon: {
    fontSize: '1.3rem',
    color: colors.shamrock,
  },
  value: {
    color: colors.shamrock,
  },
}));
