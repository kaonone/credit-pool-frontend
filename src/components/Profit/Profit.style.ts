import { makeStyles, colors } from 'utils/styles';

export const useStyles = makeStyles(() => ({
  root: {
    color: colors.shamrock,
  },

  icon: {
    position: 'relative',
    top: '.125em;',
    fontSize: 'inherit',
  },
}));
