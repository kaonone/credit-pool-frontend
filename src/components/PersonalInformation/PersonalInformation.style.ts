import { makeStyles, colors } from 'utils/styles';

export const useStyles = makeStyles(() => ({
  title: {
    fontWeight: 500,
    color: colors.topaz,
  },

  metrics: {
    color: colors.haiti,
  },
}));
