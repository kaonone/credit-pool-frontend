import { makeStyles } from 'utils/styles';

export const useStyles = makeStyles(() => ({
  root: {},

  balanceValue: {},

  graphic: {
    width: '100%',
    height: 200,
    marginTop: 24,
    marginBottom: 20,
  },

  cardContent: {
    padding: '0 !important',
  },
}));
