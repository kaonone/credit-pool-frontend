import { makeStyles } from 'utils/styles';

export const useStyles = makeStyles(() => ({
  root: {
    height: '100%',
  },

  title: {
    marginBottom: 24,
  },

  balanceValue: {},

  graphic: {
    width: '100%',
    height: 200,
    marginBottom: 20,
  },

  cardContent: {
    padding: '0 !important',
  },
}));
