import { makeStyles } from 'utils/styles';

export const useStyles = makeStyles(() => ({
  root: {},

  graphic: {
    width: '100%',
    height: 200,
    marginTop: 24,
    marginBottom: 18,
  },

  balanceValue: {
    marginTop: 17,
  },

  cardContent: {
    padding: '0 !important',
  },
}));
