import { makeStyles, colors } from 'utils/styles';

export const useStyles = makeStyles(() => ({
  root: {
    height: '100%',
  },

  title: {
    fontWeight: 500,
    color: colors.topaz,
  },

  metrics: {
    color: colors.haiti,
  },
}));
